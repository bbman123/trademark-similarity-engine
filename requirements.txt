# Trademark Similarity Engine — Requirements
# Python 3.11 | Windows (tested)
# Install: pip install -r requirements.txt
#
# GPU setup (CUDA 12.1, NVIDIA driver >= 555 — replaces base torch wheels above):
#   pip install --index-url https://download.pytorch.org/whl/cu121 \
#       torch==2.5.1+cu121 torchvision==0.20.1+cu121 torchaudio==2.5.1+cu121
#
# NOTE: TensorFlow 2.15.1 is the last stable TF build for Python 3.11 / Windows.
#       TF does NOT support native CUDA GPU on Windows after v2.10 — use CPU.
#       PyTorch/SentenceTransformer WILL use GPU if the +cu121 wheels are installed.

# -- Deep Learning -------------------------------------------------------------
tensorflow==2.15.1              # CPU only on Windows (last stable for Py3.11)
torch==2.5.1                    # CPU wheel; see GPU note above
torchvision==0.20.1
torchaudio==2.5.1

# -- NLP & Embeddings ----------------------------------------------------------
sentence-transformers==2.7.0
transformers==4.41.2
tokenizers==0.19.1
huggingface_hub==0.36.2
safetensors==0.7.0

# -- Machine Learning ----------------------------------------------------------
scikit-learn==1.8.0
scipy==1.17.0
numpy==2.4.2

# -- Data Processing -----------------------------------------------------------
pandas>=2.1.4
chardet==5.2.0
jellyfish>=1.0.3
deep-translator>=1.11.4

# -- Visualization -------------------------------------------------------------
matplotlib>=3.8.2
seaborn>=0.13.0
pillow>=10.0.0

# -- API -----------------------------------------------------------------------
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.2

# -- Utilities -----------------------------------------------------------------
python-dotenv>=1.0.0
tqdm>=4.65.0
sympy==1.13.1
filelock>=3.12.0
packaging>=23.0
requests>=2.31.0

# -- Jupyter -------------------------------------------------------------------
ipykernel>=6.25.0
ipython>=8.14.0
jupyter_client>=8.3.0
jupyter_core>=5.3.0

# -- Optional: Performance -----------------------------------------------------
# faiss-cpu>=1.7.4              # Fast nearest-neighbour search
# redis>=5.0.1                  # Distributed caching
